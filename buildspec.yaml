version: 0.2

phases:
  install:
    on-failure: ABORT
    runtime-versions:
      nodejs: latest

  build:
    on-failure: ABORT
    commands:
      - npm i
      - npm run build-static

artifacts:
  files:
    - out/*
  name: build-output