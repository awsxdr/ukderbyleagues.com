version: 0.1

phases:
  debug:
    commands:
      - ls

  install:
    on-failure: ABORT
    commands:
      - npm i

  build:
    on-failure: ABORT
    commands:
      - npm run build-static

artifacts:
  files:
    - out/*
  name: build-output