version: 0.1

phases:
  build:
    on-failure: ABORT
    runtime-versions:
      node: latest
    commands:
      - npm i
      - npm run build-static

artifacts:
  files:
    - out/*
  name: build-output